import 'package:flutter/material.dart';

class AppColors {
  // 🎨 Primary Palette
  static const Color darkNavy = Color.fromRGBO(10, 38, 71, 1);
  static const Color deepBlue = Color.fromRGBO(20, 66, 114, 1);
  static const Color mainBlue = Color.fromRGBO(32, 82, 149, 1);
  static const Color accentBlue = Color.fromRGBO(44, 116, 179, 1);

  // 🧼 Background
  static const Color backgroundLight = Color(0xFFF5F8FB);

  // ⚠️ Danger
  static const Color danger = Colors.red;

  // ✨ Optional future expansion
  static const Color white = Colors.white;
  static const Color black = Colors.black;
}
